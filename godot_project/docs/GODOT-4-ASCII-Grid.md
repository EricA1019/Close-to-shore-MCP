# ASCII Grid Addon Notes (Deprecated Old Path)

We now use the addon at `res://addons/ascii_grid/` exclusively. The previous fork under `res://addons/Godot-4-ASCII-Grid/addons/ascii_grid/` is disabled in `project.godot`.

See `docs/ASCII_RENDERING.md` for the current setup, tips, and workflow. This file remains only as a historical note about interim adjustments we made before consolidating to the attached addon.
